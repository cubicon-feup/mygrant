run.config:
  engine: nodejs
  extra_steps:
    - npm install
    - cd client && npm install && npm run build
  extra_packages:
    - nginx

data.db:
  image: nanobox/postgresql:9.5

web.main:
  start:
    nginx: nginx -c /app/config/nginx.conf
    node: npm start